```
apt-get update && apt-get upgrade -y && apt dist-upgrade -y && update-grub && reboot
```

```
sudo apt-get install gnupg -y && sudo apt install iptables && wget https://raw.githubusercontent.com/wayangkulit95/siakap/momok/installer.sh && chmod +x installer.sh && ./installer.sh
```
